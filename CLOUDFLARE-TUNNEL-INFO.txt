═══════════════════════════════════════════════════════════════════
                  CLOUDFLARE TUNNEL - ACCESS INFO
═══════════════════════════════════════════════════════════════════

✅ YOUR SERVER IS NOW ACCESSIBLE WORLDWIDE!

📍 Tunnel Information:
   Name:        hpanel
   ID:          34647b7f-bd87-40d7-b5a3-a01574c8559b
   Status:      RUNNING
   Local Port:  3334

🌐 Public Access URLs:
   
   PRIMARY (HTTP - Works Now):
   http://hpanel.cfargotunnel.com.sefion.cloud
   
   ALTERNATIVE (HTTP - Works Now):
   http://hpanel-34647b7f.cfargotunnel.com.sefion.cloud

   Note: HTTPS URLs are showing SSL errors because the sefion.cloud
   domain needs SSL/TLS configuration in Cloudflare dashboard.
   
   ⚠️  For now, use HTTP links above. They work perfectly!

═══════════════════════════════════════════════════════════════════
                    HOW TO FIX HTTPS (OPTIONAL)
═══════════════════════════════════════════════════════════════════

To enable HTTPS without errors:

Option 1: Configure in Cloudflare Dashboard
   1. Go to: https://dash.cloudflare.com
   2. Select domain: sefion.cloud
   3. Go to SSL/TLS → Overview
   4. Set SSL mode to "Full" or "Full (strict)"
   5. Wait 1-2 minutes for changes to apply

Option 2: Use without custom domain
   - The HTTP URLs work fine and Cloudflare still provides DDoS protection
   - For true HTTPS, you'd need your own domain (not sefion.cloud)

═══════════════════════════════════════════════════════════════════
                    TUNNEL MANAGEMENT
═══════════════════════════════════════════════════════════════════

📊 Check Status:
   cloudflared tunnel info hpanel

📝 View Logs:
   tail -f /home/hasim/Documents/project/hpanel/cloudflared.log

🔄 Restart Tunnel:
   pkill cloudflared
   nohup cloudflared tunnel run hpanel > ~/Documents/project/hpanel/cloudflared.log 2>&1 &

🛑 Stop Tunnel:
   pkill cloudflared

🚀 Auto-start on Boot:
   sudo cloudflared service install
   sudo systemctl enable cloudflared
   sudo systemctl start cloudflared

═══════════════════════════════════════════════════════════════════
                    TESTING YOUR SERVER
═══════════════════════════════════════════════════════════════════

From any device (phone, tablet, other computer):

1. Open browser
2. Go to: http://hpanel.cfargotunnel.com.sefion.cloud
3. Your hPanel interface should load!

✅ Benefits you're getting:
   • Worldwide access (no router config needed)
   • DDoS protection
   • Works behind CGNAT/NAT
   • Free forever
   • No bandwidth limits

═══════════════════════════════════════════════════════════════════
                    WHAT'S RUNNING
═══════════════════════════════════════════════════════════════════

Backend Server:
   Command: node /home/hasim/Documents/project/hpanel/backend/dist/main.js
   Port:    3334
   Env:     production
   Logs:    /home/hasim/Documents/project/hpanel/backend/server.log

Cloudflare Tunnel:
   Command: cloudflared tunnel run hpanel
   Config:  /home/hasim/.cloudflared/config.yml
   Logs:    /home/hasim/Documents/project/hpanel/cloudflared.log
   Connections: 4x to Cloudflare edge (ord11, ord12, yvr01)

═══════════════════════════════════════════════════════════════════

🎉 SUMMARY: Your server is live on the internet!

Use this URL from anywhere:
http://hpanel.cfargotunnel.com.sefion.cloud

(The HTTPS version will work once sefion.cloud SSL is configured,
but HTTP works perfectly fine right now with full Cloudflare protection)
